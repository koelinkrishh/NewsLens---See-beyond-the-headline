# Frontend Dashboard Dependencies
streamlit==1.54.0
streamlit-aggrid==1.2.1.post2
plotly==6.5.2
matplotlib==3.10.8
seaborn==0.13.2
wordcloud==1.9.6
altair==6.0.0
httpx==0.28.1
pandas==2.3.3
numpy==2.3.5
# Note: The frontend calls the API, so it doesn't strictly need torch/transformers
# unless it's doing local processing.
